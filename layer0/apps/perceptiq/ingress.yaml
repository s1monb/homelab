apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: perceptiq
  namespace: perceptiq
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
    - hosts:
        - perceptiq.valley.no
      secretName: perceptiq-tls
  rules:
    - host: perceptiq.valley.no
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: perceptiq
                port:
                  number: 80
