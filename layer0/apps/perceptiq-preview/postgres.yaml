apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: perceptiq-db
  namespace: PREVIEW_NAMESPACE
spec:
  instances: 1
  storage:
    size: 5Gi
    storageClass: local-path
  bootstrap:
    initdb:
      database: perceptiq
      owner: perceptiq
